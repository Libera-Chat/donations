<h1>Libera Chat Donations - Something went wrong</h1>

<p>{{message}}</p>

<p>
  If the problem persists, please contact
  <a href="mailto:support@libera.chat">support@libera.chat</a> {{#if traceId}}and include the
  request ID: <code>{{traceId}}</code>{{/if}}.
</p>

{{#if publicCtx}}
  <p>Additional context:</p>
  <pre><code>{{publicCtx}}</code></pre>
{{/if}}

<a href="{{LIBERA_CHAT_WEBSITE_URI}}/contributing/donate">Go back to the donation page</a>
